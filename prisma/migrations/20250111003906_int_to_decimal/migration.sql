BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[OldSession] ALTER COLUMN [rate] DECIMAL(32,16) NOT NULL;

-- AlterTable
ALTER TABLE [dbo].[Sentence] ALTER COLUMN [similarity] DECIMAL(32,16) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
