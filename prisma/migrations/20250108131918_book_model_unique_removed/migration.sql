BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[Book] DROP CONSTRAINT [Book_bookName_key];

-- DropIndex
ALTER TABLE [dbo].[Book] DROP CONSTRAINT [Book_imagePath_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
